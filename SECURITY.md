# 安全说明

## 支持范围

当前仅维护仓库主分支的最新版本。项目仍处于早期发布阶段，尚未承诺长期安全支持周期。

以下问题属于安全范围：

- Cookie、Authorization header、签名 URL 或其他凭据泄露；
- 路径穿越、符号链接逃逸或对 asset 目录外文件的意外读写；
- 清理命令删除声明范围外的文件；
- 日志、manifest、Demo 或错误信息暴露私有媒体内容和本地身份信息；
- 外部证据绕过来源隔离，被错误标记为视频事实。

## 报告漏洞

请优先使用 GitHub Security Advisory 的私密报告功能。若仓库尚未启用该功能，请联系维护者建立私密沟通渠道，不要在公开 Issue 中粘贴漏洞细节、Cookie、私有 URL、日志或媒体样本。

报告中请提供：

- 受影响的版本或 commit；
- 最小复现步骤；
- 预期行为与实际行为；
- 可能影响的文件和数据范围；
- 已做脱敏的错误信息。

维护者确认问题后会评估影响、准备修复并协调披露时间。项目当前不承诺固定响应时限。

## 使用者责任

- 默认只处理公开内容；需要 Cookie 时必须取得内容所有者或使用者的明确授权。
- 不要把 Cookie、浏览器 profile、私有媒体、原始日志或本地绝对路径提交到仓库。
- 执行清理前先运行 `cleanup-l2 --dry-run` 并检查目标范围。
- 模型输出、外部证据和生成 PDF 在对外发布前必须经过事实、隐私与版权审核。

安全修复可能调整 schema、CLI 验证或保留策略。升级后请重新运行相关测试和资产验证。
